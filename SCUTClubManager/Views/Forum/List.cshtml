@model IEnumerable<SCUTClubManager.Models.Thread>

@{
    ViewBag.Title = "Index";
}

<h2>留言板</h2>

<div>
    @Html.ActionLink("发表新话题", "Create")
    @Html.Partial("_SearchBar")
</div>
<table>
    <tr>
        <th>
            标题
        </th>
        <th>
            作者
        </th>
        <th>
            @Html.ActionLink("发表日期","List", new { order = ViewBag.PostDateOpt })
        </th>
        <th>
            @Html.ActionLink("最后回复日期","List",new { order  =  ViewBag.LatestReplyDateOrderOpt})
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.Title, "Details", new { id=item.Id }) 
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Author.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Replies.First().Date);
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Replies.Last().Date);
        </td>
        <td>
            @Html.ActionLink("删除", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

@Html.Partial("_PageNavigation", Model)