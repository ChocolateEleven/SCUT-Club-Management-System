@if(Request.IsAuthenticated) {
    <text>你好， <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("登出", "LogOut", "Home") ]</text>
}
else {
    @:[ @Html.ActionLink("登录", "LogIn", "Home") ]
}
